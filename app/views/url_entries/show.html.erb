<p style="color: green"><%= notice %></p>

<%= render @url_entry %>

<div>
  <%= link_to "Back to url entries", root_path %> |
  <%= link_to "Edit this url entry", edit_url_entry_path(@url_entry) %> |
  <%= button_to "Destroy this url entry", @url_entry, method: :delete, :onclick => "return confirm('This will delete this link, are you sure?')" %>
</div>
